<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/header.ejs') %>
   <title>OTROS</title>
</head>
<body>
    <%- include('../partials/nav') %>

    <section class="main">
        <h2 class="m-5">OTROS</h2>

        <div id="datatables" class="m-5 mx-auto">
            <table id="mytable" class="table table-bordered table-hover table-stripped">
                <thead>
                    <tr>
                        <th>NAME</th>
                        <th>ACCOUNT</th>
                        <th>DESCRIPTION</th>
                        <th>WEBSITE</th>
                        <th>EMAIL</th>
                        <th>PHONE</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    <% for (let i = 0; i < otros.length; i++){ %>
                        <tr>
                            <td><%= otros[i].name %></td>
                            <td><%= otros[i].account %></td>
                            <td><%= otros[i].description %></td>
                            <% if (otros[i].website_name != ' - ') { %>
                                <td><a href="<%= otros[i].website_link %>"><%= otros[i].website_name %></a></td>
                            <% } else { %>
                                <td>None</td>
                            <% } %>
                            <td><%= otros[i].email %></td>
                            <td><%= otros[i].phone %></td>
                            <td>
                                <form action="/users/addtofav/<%= otros[i].id %>" method="post">
                                    <input type="hidden" name="selectedRow" value="<%= otros[i].id %>">
                                    <button class="btn btn-success" type="submit">Add to Favorites</button>
                                </form>
                            </td>
                        </tr>
                    <% } %>

                </tbody>
                <tfoot>
                    <tr>
                        <th>NAME</th>
                        <th>ACCOUNT</th>
                        <th>DESCRIPTION</th>
                        <th>WEBSITE</th>
                        <th>EMAIL</th>
                        <th>PHONE</th>
                        <th></th>
                    </tr>
                </tfoot>
            </table>
        </div>
    </section>


    <%- include('../partials/footer.ejs') %>

</body>
</html>